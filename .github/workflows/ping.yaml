name: Keep Render Alive
on:
  workflow_dispatch: # מאפשר הרצה ידנית מתוך GitHub
  schedule:
    - cron: '*/10 * * * *'

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping App
        run: curl -s "https://whatsapptoosx.onrender.com/webhook"